__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# data/raw, data/features, data/fundamentals, data/published are committed
# as a point-in-time snapshot so the Streamlit Cloud deploy has something to
# show. Re-run scripts/run_daily_scan.py + scripts/build_universe_snapshot.py
# and commit again to refresh.
